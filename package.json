{
  "name": "gantt-chart-canvas",
  "version": "2.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "yarn build-lib && yarn build-demo",
    "build-lib": "esbuild src/GanttChart.js --outfile=dist/GanttChart.js --bundle --minify --target=chrome101,firefox99",
    "build-demo": "esbuild demo/index.js --bundle --outfile=dist/index.js --minify"
  },
  "keywords": [
    "gantt",
    "chart",
    "canvas"
  ],
  "author": "Artem Shubovych <shybovycha@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.14.38"
  },
  "dependencies": {
    "date-fns": "^2.19.0"
  }
}
